name: Deployment
on:
  push:
    branches:
      - main
      - dev
env:
  user_value: Alisan_Celik
jobs:
  test:
    environment: testing
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v3
      - name: Greating
        run: echo "testing was done!!!"
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run:  echo "Kullanici Adi:${{ env.user_value }}"
